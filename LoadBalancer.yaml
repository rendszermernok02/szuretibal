apiVersion: v1
kind: Service
metadata:
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-resource-group: rg5
  name: testhtml
spec:
  loadBalancerIP: x.x.x.x
  type: LoadBalancer
  ports:
  - port: 80
  selector:
    app: testhtml
